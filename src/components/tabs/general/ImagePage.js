import {memo, useEffect} from "react";

export const ImagePage = memo(({ws}) => {

    let img;

    useEffect(() => {
      // setTimeout(() => {
      //     ws.getImage().then(image => console.log(image)).catch(err => console.log(err))
      // },2000)
    }, [])

    return (
        <></>
    )
})

